# Backend URL used by Next.js API proxy routes (server-side)
BACKEND_URL=http://127.0.0.1:8086

# Backend URL used by the browser for OAuth login redirect (client-side)
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8086
