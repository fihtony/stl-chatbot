# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# render (optional - you can commit render.yaml)
# .render

# typescript
*.tsbuildinfo
next-env.d.ts

# service account keys
*.json
!package.json
!tsconfig.json
!tsconfig*.json
!vercel.json
!ecosystem.config.js
service-account*.json
*-account*.json

# scraped data
/data/scraped/
!data/scraped/.gitkeep
# OCR cache (old JSON format, generated files, don't commit)
/data/scraped/ocr-cache/
# PDF texts (OCR results as .txt files, generated files, don't commit)
/data/scraped/pdf-texts/

# logs
/logs/

# OCR training data (large files)
*.traineddata

# Test files and scripts (not needed for production)
test-*.ts
scripts/test-*.ts
scripts/test-*.ps1
debug-files.ts
start-crawler.sh
ecosystem.config.js

# Documentation (personal notes, not needed for deployment)
docs/AWS_BILLING_*.md
docs/CHECK_RESOURCES.md
docs/CLEAN_START.md

# Service account files (should be in environment variables)
college-saint-louis-docs-*.json
service-account-oneline.json

